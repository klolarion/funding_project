Domain (도메인 계층):

구성 요소: model, repository, service

역할: 시스템의 비즈니스 규칙을 표현하는 계층으로,
    엔티티(Entity),
    값 객체(Value Object),
    애그리게잇(Aggregate),
    도메인 서비스(Domain Service),
    리포지토리 인터페이스(Repository Interface) 등이 포함됩니다.
특징: 이 계층은 애플리케이션의 핵심 비즈니스 로직을 포함하며, 외부 환경에 대한 의존성이 없어야 합니다.


레이어드 아키텍처의 model 또는 entity, service